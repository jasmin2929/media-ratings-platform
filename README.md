# media-ratings-platform
A standalone HTTP-based REST server built in Java that implements secure user authentication, media management, rating, and recommendation logic, backed by a PostgreSQL database.
